export enum StoryStatus {
  ONGOING = 'ongoing',
  COMPLETED = 'completed',
  PAUSED = 'paused',
}

export enum StoryPublishStatus {
  DRAFT = 'draft',
  PENDING_REVIEW = 'pending_review',
  PUBLISHED = 'published',
  REJECTED = 'rejected',
}