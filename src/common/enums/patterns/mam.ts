export enum MAM_Patterns {
  QUERY_TASKS = 'query-tasks',
  UPDATE_TASK_RESULT = 'update-task-result',
  MONITORING_TASKS = 'monitoring-tasks',
  SEND_NOTIFICATION = 'send-notification',
  SEND_NODE_DATA = 'send-node-data',
}
