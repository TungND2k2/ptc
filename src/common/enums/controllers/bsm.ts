export enum BSM_Controllers {
  BSM_Provider = 'providers',
  BSM_Bot = 'bots',
  BSM_Account = 'accounts',
  BSM_Campaign = 'campaigns',
  BSM_Task = 'tasks',
  BSM_Proxy = 'proxies',
  BSM_Report = 'reports',
  BSM_Log = 'logs',
}
